# Replit Integration Guide - V7 Final (3 Categories)

## 3-Category Framework

| Category | Color | Description |
|----------|-------|-------------|
| 🟢 **Human-Centric** | Green | Tasks requiring human presence, judgment, empathy |
| 🔵 **AI-Assisted** | Blue | Complex work where AI tools help but humans lead |
| 🟠 **Automation** | Orange | Tasks AI agents can handle or fully automate |

> **Automation** = Agentic AI (agents, copilots) + Traditional Automation (routine tasks)

---

## Files

| File | Description |
|------|-------------|
| `jobs_v7_3cat.json` | 2,027 jobs with 3-category breakdown + aliases |

---

## Data Structure

```json
{
  "id": "43104-132",
  "de": "Data Scientist",
  "en": "Data Scientist",
  "sector": "IT & Science",
  "human": 21.5,
  "ai_assisted": 70.7,
  "automation": 7.8,
  "dominant": "AI-Assisted",
  "aliases": ["Data Engineer", "Analytics Engineer", "ML Engineer"]
}
```

---

## JavaScript Integration

### Load Data
```javascript
let jobs = [];

async function loadJobs() {
  const response = await fetch('/data/jobs_v7_3cat.json');
  jobs = await response.json();
  console.log(`Loaded ${jobs.length} jobs`);
}
```

### Search Function
```javascript
function searchJobs(query) {
  const q = query.toLowerCase().trim();
  if (!q || q.length < 2) return [];
  
  const results = [];
  
  // Priority 1: Exact alias match
  for (const job of jobs) {
    const aliases = job.aliases || [];
    if (aliases.some(a => a.toLowerCase() === q)) {
      results.push({ ...job, matchType: 'alias_exact' });
    }
  }
  
  // Priority 2: Alias contains query
  for (const job of jobs) {
    if (results.some(r => r.id === job.id)) continue;
    const aliases = job.aliases || [];
    if (aliases.some(a => a.toLowerCase().includes(q))) {
      results.push({ ...job, matchType: 'alias_partial' });
    }
  }
  
  // Priority 3: Title contains query
  for (const job of jobs) {
    if (results.some(r => r.id === job.id)) continue;
    if (job.en.toLowerCase().includes(q) || job.de.toLowerCase().includes(q)) {
      results.push({ ...job, matchType: 'title' });
    }
  }
  
  return results.slice(0, 15);
}
```

### Display Component (React)
```jsx
const COLORS = {
  'Human-Centric': '#22c55e',  // green
  'AI-Assisted': '#3b82f6',    // blue
  'Automation': '#f59e0b',     // orange
};

const LABELS = {
  'Human-Centric': 'Human-Centric',
  'AI-Assisted': 'AI-Assisted', 
  'Automation': 'Automation (Agents + Routine)',
};

function JobProfile({ job }) {
  return (
    <div className="job-profile">
      <h2>{job.en}</h2>
      <span className="sector">{job.sector}</span>
      
      <div className="profile-bars">
        {job.human > 0 && (
          <div 
            className="bar" 
            style={{ width: `${job.human}%`, background: COLORS['Human-Centric'] }}
          >
            Human {job.human}%
          </div>
        )}
        {job.ai_assisted > 0 && (
          <div 
            className="bar" 
            style={{ width: `${job.ai_assisted}%`, background: COLORS['AI-Assisted'] }}
          >
            AI-Assisted {job.ai_assisted}%
          </div>
        )}
        {job.automation > 0 && (
          <div 
            className="bar" 
            style={{ width: `${job.automation}%`, background: COLORS['Automation'] }}
          >
            Automation {job.automation}%
          </div>
        )}
      </div>
      
      <p className="dominant">
        Primarily: <strong>{job.dominant}</strong>
      </p>
    </div>
  );
}
```

---

## Category Definitions (for tooltips/info)

```javascript
const CATEGORIES = {
  'Human-Centric': {
    color: '#22c55e',
    icon: '👤',
    title: 'Human-Centric',
    description: 'Tasks requiring human presence, judgment, empathy, or physical interaction',
    examples: 'Patient care, negotiations, teaching, creative direction'
  },
  'AI-Assisted': {
    color: '#3b82f6',
    icon: '🤝',
    title: 'AI-Assisted',
    description: 'Complex work where AI tools enhance productivity but humans lead decision-making',
    examples: 'Strategic analysis, architecture design, complex problem-solving, research'
  },
  'Automation': {
    color: '#f59e0b',
    icon: '🤖',
    title: 'Automation',
    description: 'Tasks that AI agents can handle autonomously or routine tasks suitable for full automation',
    examples: 'Code generation, data processing, scheduling, report writing, data entry'
  }
};
```

---

## Statistics

### Dominant Category Distribution
| Category | Jobs | % |
|----------|------|---|
| Human-Centric | 1,406 | 69% |
| AI-Assisted | 554 | 27% |
| Automation | 67 | 3% |

### Automation Exposure
| Threshold | Jobs |
|-----------|------|
| Any automation (>0%) | 879 (43%) |
| Significant (>30%) | 141 (7%) |
| High (>50%) | 37 (2%) |

---

## Sample Profiles

### Data Scientist
```
Human-Centric:  21.5%  (stakeholder communication)
AI-Assisted:    70.7%  (analysis, modeling, strategy)
Automation:      7.8%  (data processing, code snippets)
→ Dominant: AI-Assisted
```

### Recruiter
```
Human-Centric:  25.0%  (interviews, relationship building)
AI-Assisted:    12.5%  (candidate evaluation)
Automation:     62.5%  (screening, outreach, scheduling)
→ Dominant: Automation
```

### Registered Nurse
```
Human-Centric: 100.0%  (patient care, empathy)
AI-Assisted:     0.0%
Automation:      0.0%
→ Dominant: Human-Centric
```

### Software Developer
```
Human-Centric:  22.9%  (collaboration, code review)
AI-Assisted:    64.2%  (architecture, complex problems)
Automation:     12.9%  (boilerplate code, testing)
→ Dominant: AI-Assisted
```

---

## Alias Coverage

Modern job titles that work via search:

| Search | → Maps To |
|--------|-----------|
| ux designer | User Interface Developer |
| frontend | Mobile Developer |
| backend | Software Developer |
| cto | Technical Managing Director |
| cfo | Commercial Managing Director |
| customer success | Account Manager |
| partnership | Business Development Manager |
| hr manager | Head of Human Resources |
| data engineer | Data Scientist |
| founder | Executive Board Member/CEO |

---

## Methodology (for About page)

```
This tool analyzes AI exposure for 2,027 German white-collar occupations 
using a 3-category framework:

• Human-Centric: Tasks requiring human presence, judgment, or empathy
• AI-Assisted: Complex work where AI tools help but humans lead
• Automation: Tasks AI agents can handle or routine tasks for automation

The "Automation" category combines:
- Agentic AI potential (tasks for AI agents like coding assistants)
- Traditional automation (routine digital tasks)

Data sources:
- German Federal Employment Agency (KldB 2010) for task definitions
- O*NET (US Department of Labor) for interpersonal skill requirements
```

---

## Quick Test Checklist

- [ ] Search "data scientist" → 21.5% Human, 70.7% AI-Assisted, 7.8% Automation
- [ ] Search "recruiter" → 62.5% Automation (high!)
- [ ] Search "nurse" → 100% Human-Centric
- [ ] Search "ux designer" → finds User Interface Developer
- [ ] Search "cto" → finds Technical Managing Director
